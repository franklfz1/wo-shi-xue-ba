# 系统搭建指南

> 面向新用户：从零开始搭建「我是学霸」AI 辅助学习系统。

---

## 第一部分：环境准备

### 1.1 安装 Python

需要 Python 3.10+。推荐使用官方安装包或 conda：

```bash
# 验证安装
python --version
```

### 1.2 安装依赖包

```bash
pip install flask flask-cors edge-tts pillow numpy scipy
```

- `flask` + `flask-cors`：看板后台 API
- `edge-tts`：视频配音生成
- `pillow` + `numpy` + `scipy`：视频画面渲染和音效生成

### 1.3 安装 FFmpeg（视频制作需要）

```bash
# Windows
winget install ffmpeg

# macOS
brew install ffmpeg

# Linux
sudo apt install ffmpeg
```

验证：`ffmpeg -version`

### 1.4 安装 Playwright（SVG 动画帧捕获需要）

```bash
pip install playwright
playwright install chromium
```

### 1.5 安装 WorkBuddy Desktop（前提条件）

本系统的 AI 老师功能依赖 WorkBuddy 作为运行环境：

1. **下载**：https://www.codebuddy.cn/download
2. **安装**：按提示完成安装
3. **打开项目**：启动 WorkBuddy，将 `wo-shi-xue-ba` 目录设为工作目录
4. **Skill 自动加载**：WorkBuddy 会自动识别 `skills/` 目录下的内置 Skill

> 也支持其他 AI 工具（Claude、ChatGPT 等），但需手动复制 `teachers/*.md` 提示词到对话中，且无法使用内置 Skill 的自动化功能（如学科初始化、自动出题等）。

---

## 第二部分：项目初始化

### 2.1 克隆或下载项目

**方式 A — git clone**：

```bash
git clone https://github.com/franklfz1/wo-shi-xue-ba.git
cd wo-shi-xue-ba
```

**方式 B — 下载 ZIP**：

在 GitHub 页面点击 "Code → Download ZIP"，解压到任意目录。

**方式 C — 一键安装脚本**：

下载项目后运行安装脚本（会自动安装依赖 + 初始化目录）：

```bash
# Windows
install.bat

# macOS / Linux
chmod +x install.sh && ./install.sh
```

### 2.2 一键初始化（⭐ 关键步骤）

安装完依赖后，在 WorkBuddy 对话中说：

> **初始化**

系统会自动完成全部初始化流程：

1. **环境检查**：验证 Python、依赖包、FFmpeg 是否就绪
2. **产物目录初始化**：自动运行 `python artifacts_init.py --init`
3. **选择年级**：弹出选项卡，先选学段（学前/小学/初中/高中），再选具体年级
4. **选择科目**：弹出选项卡（多选），勾选需要初始化的科目（数学/语文/英语/科学/地理/物理）
5. **逐科配置**：对每个选中的科目弹出选项卡问答，收集 4 项配置：

| 配置项 | 说明 |
|--------|------|
| 定位类型 | 跳级衔接型 / 拓展提升型 / 书面内容导向型 / 兴趣驱动型 |
| 教材版本 | 人教版/统编版 + 年级上下册 |
| 当前水平 | 零基础 / 略有了解 / 基础扎实 / 超前学习 |
| 教学风格 | 活泼探险型 / 温暖陪伴型 / 严谨教练型 / 幽默启发型 |

6. **自动生成**：每科生成定制版提示词 + `todo.md`（从知识库读取知识点）+ 看板卡片 + 复习配置 + FAQ
7. **清理未选学科**：未选的学科模板自动删除（提示词/记录目录/看板卡片/复习配置），保持系统干净
8. **启动看板**：自动运行 `data_api.py` 并打开浏览器
9. **设置每日自动化任务**：弹出选项卡配置两个定时任务：
   - **每日复习提醒**（默认 9:00）— 自动检查 `review/next-review.json`，提醒今日到期的知识点和错题复习内容
   - **每日学习总结**（默认 22:00）— 总结当日学习情况，核实并更新 `mastered.md`、`errors.md`、复习计划

整个流程约 5-15 分钟（取决于选了几科）。完成后说"切换成数学老师"即可开始学习。自动化任务会在后台每天按时执行，无需手动干预。

> 💡 只想配置单科？说"初始化数学老师"即可（会额外询问年级）。想重新配置某科？说"重新初始化数学老师"。

### 2.3 手动初始化（不使用 Skill）

如果不用 WorkBuddy 或想手动操作：

1. 运行 `python artifacts_init.py --init`
2. 编辑 `teachers/*.md`，将"当前学生水平"部分改为孩子实际情况
3. 参考 `knowledge-base/` 中的教材目录，将知识点填入 `records/<学科>/todo.md`：
   ```markdown
   ## 待学知识点

   - ⬜ 100以内数的认识
   - ⬜ 100以内的加法和减法
   - ⬜ 认识人民币（元角分）
   ```
4. 确认 `review/next-review.json` 的 `subjects` 数组包含所有学科
5. 运行 `python data_api.py` 启动看板

---

## 第三部分：配置与个性化

### 3.1 选择学科定位

每个学科有不同定位类型，影响教学节奏和深度：

| 学科 | 默认定位 | 说明 |
|------|----------|------|
| 数学 | 跳级衔接型 | 从一年级下册或更高起点开始 |
| 语文 | 拓展提升型 | 认写分离，四线并进 |
| 英语 | 书面内容导向型 | 不教发音，只教书面认读 |
| 科学 | 兴趣拓展型 | 主题探索，无固定大纲 |
| 地理 | 兴趣拓展型 | 五大探索主题库 |
| 物理 | 跳级衔接型 | 初中物理框架，八年级起点 |

> 💡 以上是系统预设定位。如果你在 2.2 节已完成初始化，这些配置已经通过选项卡问答自动完成了。如需手动调整，修改对应 `teachers/*.md` 文件的角色定位和知识体系部分。

### 3.2 调整待学知识点

如果你在 2.2 节已完成初始化，各科的 `todo.md` 已经从教材知识库自动填充了知识点。

如需手动调整（增删知识点或调整顺序），直接编辑 `records/<学科>/todo.md`。建议参考 `knowledge-base/` 中的教材知识库来组织知识点顺序。

### 3.3 调整自动化任务时间（可选）

初始化时已自动创建两个每日自动化任务（复习提醒 + 学习总结）。如需调整时间或关闭：

- 说"修改复习提醒时间为早上8点"或"修改学习总结时间为晚上9点"
- 或在 WorkBuddy 设置 → 自动化 中手动编辑/暂停对应任务

**默认配置**：
- 每日复习提醒：检查 `review/next-review.json` 和 `review/errors-review.json`，输出今日到期复习清单
- 每日学习总结：总结当日学习情况，更新 `mastered.md`、`errors.md`、推进复习计划轮次

### 3.4 腾讯文档同步（可选）

如果你需要将学习进度同步到腾讯文档，可以配置：

1. 安装腾讯文档 MCP 连接器（WorkBuddy 设置中启用）
2. 创建一个腾讯文档作为"学习中心"
3. 运行 `python sync_to_tencent.py` 进行手动同步，或设置自动化定时同步

---

## 第四部分：启动与使用

### 4.1 启动看板

```bash
python data_api.py
```

浏览器自动打开 `http://localhost:5177/`，显示学习进度看板。

看板包含：
- 六科进度环形图
- 各科卡片（已掌握列表、易错点）
- 今日复习任务（可交互勾选完成）
- 月历视图

Windows 用户也可以双击 `启动看板.bat`。

### 4.2 开始学习

在 AI 对话中，输入学科切换命令：

- **"切换成数学老师"** → AI 加载 `teachers/math.md` 提示词，以数学老师角色教学
- **"切换成语文老师"** → 同理
- 其他学科：英语、科学、地理、物理

AI 老师会：
1. 引导式教学（先提问，后讲解）
2. 配图讲解每个概念
3. 实时出题检验理解
4. 标记已掌握/易错知识点
5. 自动建立复习计划

### 4.3 生成练习题

```bash
# 全科各一份（每份8道）
python exercise_engine.py

# 只生成数学
python exercise_engine.py --subject math

# 指定题数和纸张
python exercise_engine.py -s math -c 10 --paper A3

# 手动指定知识点
python exercise_engine.py -s chinese -t "古诗词|生字书写"
```

输出文件保存在 `artifacts/exercises/` 目录，可直接打印。

### 4.4 完成复习

每日查看看板的"今日复习任务"，点击完成按钮即可推进艾宾浩斯轮次。

---

## 第五部分：新增学科

系统支持任意扩展新学科。

### 推荐方式：使用 AI 自动新增

在 WorkBuddy 对话中说：

> 初始化历史老师

或

> 新增化学老师

系统会加载 `skills/teacher-prompt-creator` skill，通过选项卡问答收集配置（学科名/定位/教材/水平/风格），自动完成提示词生成 + 学科初始化（records + 看板 + 复习配置 + todo.md 填充）。

### 手动方式

如不使用 Skill，按以下步骤操作：

1. **复制模板**：将 `system/teacher-prompt-template.md` 复制为 `teachers/{新学科}.md`
2. **填写提示词**：遵循 `system/teacher-prompt-rules.md` 的十一大模块规范
3. **创建记录目录**：在 `records/{新学科}/` 下创建 `mastered.md` + `todo.md` + `errors.md` + `review-log.md`
4. **更新代码**：在 `data_api.py` 的 `subjects` 列表和 `subject_labels` 字典中添加新学科
5. **更新看板**：`dashboard.html` 的 `SUBJECTS` 配置中添加新学科卡片
6. **初始化产物目录**：`python artifacts_init.py --subject {新学科}`

### 提示词编写规则速查

十一大必含模块（详见 `system/teacher-prompt-rules.md`）：

1. **角色定位**：教学风格、语气、比喻体系
2. **教学风格**：具体教学行为规范
3. **图文交互教学规则**：配图/交互控件/一张图一概念
4. **知识体系**：分阶段的知识点列表
5. **常见易错点**：学生容易犯的错误清单
6. **掌握判断标准**：怎样才算"学会了"
7. **练习出题模式**：五级难度 + 出题优先级 + 自检流程
8. **学习记录格式**：mastered/errors/review-log 的具体格式
9. **知识边界与防幻觉规则**：不确定时不编造
10. **教学摘要记录规则**：每次教学后自动记录
11. **切换提示**：触发词定义
+ **学科禁忌清单**：该学科禁止教授的内容

---

## 第六部分：视频制作流水线

### 6.1 概述

`video-pipeline/` 是完整的自动化教学短视频制作工具链：

1. 读取教学脚本 → 2. edge-tts 生成配音 → 3. Python PIL 渲染画面帧 → 4. FFmpeg 合成视频

### 6.2 配置

`video-pipeline/config.json` 包含所有参数：
- 视频尺寸：1080×1920（竖屏），30fps
- 配音：zh-CN-XiaoxiaoNeural
- 字幕：微软雅黑，白色带黑边

注意：`ffmpeg.path` 默认设为 `"ffmpeg"`（使用系统 PATH 中的 ffmpeg）。如果你的 ffmpeg 不在 PATH 中，需要修改为绝对路径。

### 6.3 使用

```bash
cd video-pipeline
python scripts/main_pipeline.py
```

---

## 第七部分：架构详解

### 四层分离架构

```
┌─────────────── 用户交互层 ────────────────┐
│  dashboard.html  │  AI老师对话  │  出题引擎  │
└────────────┬─────────────┬──────────┬──────┘
             │             │          │
             ▼             ▼          ▼
┌─────────────── API 层 (data_api.py) ───────┐
│  /api/dashboard  │  /api/complete-review    │
│  /api/add-mastered  │  /api/add-error       │
└────────────┬───────────────────────────────┘
             │ 读写
             ▼
┌─────────────── 数据层 (本地文件) ──────────┐
│  review/*.json  │  records/{科}/*.md        │
│  artifacts/     │  audio/                   │
└────────────┬───────────────────────────────┘
             │ 指导
             ▼
┌─────────────── 知识层 (教学内容) ──────────┐
│  teachers/*.md  │  knowledge-base/*.md      │
│  question-bank/ │  system/knowledge-anchors │
└────────────────────────────────────────────┘
```

### 三层知识体系

| 层级 | 目录 | 职责 | 示例 |
|------|------|------|------|
| 锚点库 | `system/knowledge-anchors/` | 管"对不对" | 乘法口诀、古诗原文 |
| 知识库 | `knowledge-base/` | 管"教什么" | 教材单元结构、教学目标 |
| 习题库 | `question-bank/` | 管"练什么" | 数学一年级 500 题 |

### 艾宾浩斯复习引擎

- **知识点复习**：6轮间隔（1-2-4-7-15-30天）
- **错题复习**：5轮间隔（1-2-3-5-7天）
- 标记掌握 → 自动建立复习计划 → 每日到期提醒 → 完成复习推进轮次 → 全轮次完成标记为 done

---

## 第八部分：常见问题

### Q: 看板打不开？

检查 `data_api.py` 是否在运行：
```bash
curl http://localhost:5177/api/status
```

如果返回 `{"ok": true, ...}` 说明服务正常。否则重新启动：

```bash
python data_api.py
```

### Q: AI 老师的发音不准确？

英语老师只教书面内容，不教发音。语音部分由家长通过其他方式补充（原版音频、外教等）。

### Q: 如何清空学习数据重新开始？

删除 `records/*/mastered.md` 和 `review/*.json` 中的内容，重新填写 `todo.md`。

### Q: 如何备份学习数据？

所有数据都是 Markdown 和 JSON 文件，直接复制 `records/` 和 `review/` 目录即可。

### Q: 视频制作需要什么环境？

- Python 3.10+ with pillow/numpy/scipy
- ffmpeg 在系统 PATH 中
- edge-tts pip 包
- Playwright（可选，用于 SVG 动画帧捕获）

---

## 第九部分：贡献指南

欢迎贡献！你可以：

1. **新增学科提示词**：按模板规范写新学科的老师提示词
2. **扩充题库**：为 `question-bank/` 添加更多学科的 JSONL 题库
3. **完善知识库**：补充更多年级/学科的教材知识库
4. **校验锚点库**：对照权威资料校验事实锚点的准确性
5. **改进看板**：优化 `dashboard.html` 的可视化效果
6. **优化视频流水线**：改进画面模板、添加新特效

提交 PR 时请确保：
- 不包含任何个人学习数据
- 遵循现有的目录结构和命名规范
- 新增的提示词遵循 `system/teacher-prompt-rules.md` 规范
